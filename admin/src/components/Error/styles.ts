import styled from 'styled-components'
import { color } from '../../utils/styles'

export const Container = styled.div`
  display: flex;
  align-items: center;
  gap: 5px;
  font-size: 14px;
  color: ${color.danger};
`
